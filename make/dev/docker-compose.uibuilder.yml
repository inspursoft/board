version: '2'
services:
  uibuilder:
    build:
      context: ../../
      dockerfile: make/dev/container/uibuilder/Dockerfile
    image: uibuilder
    volumes:
      - ../../src/ui:/board_src
    environment:
      MODE: build
    entrypoint:
      - /entrypoint.sh
