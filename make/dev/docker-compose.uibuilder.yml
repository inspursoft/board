version: '2'
services:
  uibuilder:
    build:
      context: ../../
      dockerfile: make/dev/container/uibuilder/Dockerfile
    image: dev_uibuilder:dev
    volumes:
      - ../../src/ui:/board_src
    entrypoint:
      - /entrypoint.sh
