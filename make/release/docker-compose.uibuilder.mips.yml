version: '2'
services:
  uibuilder-mips7.1:
    build:
      context: ../../
      dockerfile: make/dev/container/uibuilder/Dockerfile.mips
    image: uibuilder-mips7.1
    volumes:
      - ../../src/ui:/board_src
    environment:
       MODE: prod
    entrypoint:
      - /entrypoint.sh
